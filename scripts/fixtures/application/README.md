# Application verifier fixtures

These small records cover one carrier and eight requests. The server fixture omits its event
stream and command and retains one sampled resource record. The mutation tests use them to
reject incomplete or inconsistent application receipts.
